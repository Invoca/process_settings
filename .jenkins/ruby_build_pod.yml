---
apiVersion: v1
kind: Pod
metadata:
  labels:
    jenkins/process_settings: 'true'
  namespace: jenkins
  name: process_settings
spec:
  containers:
  - name: ruby
    image: ruby:2.6.5
    tty: true
    resources:
      requests:
        memory: "500Mi"
      limits:
        memory: "200Mi"
    command:
      - cat
